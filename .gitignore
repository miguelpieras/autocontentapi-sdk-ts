node_modules/
# Keep the compiled package in Git so browser/server consumers can pin this
# public repository without running lifecycle scripts during installation.
.DS_Store
*.log
.env
.env.*
!.env.example
